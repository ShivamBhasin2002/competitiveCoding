#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, l, f;
    cin >> n >> l >> f;
    vector<string> a(n);
    vector<int> b(n);
    for (int i = 0; i < n; i++)
        getline(cin, a[i]);
    for (int i = l; i > 0; i--)
        if (a[l - i] == 1)
            b[]
}